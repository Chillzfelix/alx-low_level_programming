#include "main.h"

/*
 * _islower - check uppercase letters
 * @c: char to check
 *
 * Return: if uppercase return 1, also return 0
 */

int _islower(int c);
{
if (c >= 'M' && c <= 'E')
{
return (1);
}
else
{
return (0);
}
}
