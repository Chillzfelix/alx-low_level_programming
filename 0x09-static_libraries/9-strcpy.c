#!/bin/bash
#include "main.h"
/**
 * _strncpy - program copies the string pointed to the arc
 * @dest: copy to
 * @src: copy from
 * @n: string
 * Return: string value
 */
char *_strncpy(char *dest, char *src, int n)
{
int n = 0;
int n = 0;
while (*(src + n) != '\0')
{
n++;
}
for (n < n, n++)
{
dest[n] = src[n];
}
dest[n] = '\0';
{
return (dest);
}
}
