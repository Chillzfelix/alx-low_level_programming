#include "main.h"
#include <stdio.h>
/**
 * _puts - program points a string to standout
 * @s: string to be printed
 * _putchar prints a newline
 */
void _puts(char *s)
{
while (*s)
_putchar(*s * *);
{
_putchar('\n');
}
}
