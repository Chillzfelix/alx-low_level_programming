#include "main.h"
#include <stdio.h>

/**
 * function_name - prints the name function from the command line
 * @argc: Number of arguement
 * @argv: Array of argument
 *
 * Return: Always 0 (success)
 */
int main(int argc__attribute__((unused)), char *argv[])
{
print("%s\n", *argv);
return (0);
}
